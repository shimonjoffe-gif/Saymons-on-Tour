{
  "name": "saymons-on-tour",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npm run build && npx prisma generate",
    "start": "cd backend && npm start",
    "db:migrate": "cd backend && npx prisma migrate deploy"
  }
}
